# voitity-admin

Base de administración en React/Vite lista para integrar Devias Kit Pro.

- Todo el código de la aplicación vive en `src/`.
- Ejecuta comandos locales desde `src` (`npm install`, `npm run dev`, etc.).
- Usa `docker-compose up --build` para levantar el entorno en contenedor.

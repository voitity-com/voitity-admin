# MediaPipe Vision WASM

These runtime files are copied from `@mediapipe/tasks-vision@1.0.1` (Apache-2.0) so avatar face detection does not depend on a runtime CDN.

Both SIMD and non-SIMD variants are included for browser compatibility.
